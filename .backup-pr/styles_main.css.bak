@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap');

body {
    font-family: 'Inter', sans-serif;
}

.blog-card {
    @apply bg-white rounded-xl shadow-md overflow-hidden transition-all duration-300 hover:shadow-xl hover:-translate-y-1;
}

.card-link {
    @apply block;
}

.card-image {
    @apply w-full h-48 object-cover;
}

.card-content {
    @apply p-6;
}

.card-category {
    @apply block text-sm font-semibold text-blue-600 uppercase tracking-wide;
}

.card-title {
    @apply mt-2 text-xl font-bold text-slate-900 leading-tight;
}

.card-summary {
    @apply mt-2 text-slate-600 text-base;
}

.card-footer {
    @apply mt-4 flex items-center justify-between text-sm text-slate-500;
}

.card-author {
    @apply font-medium;
}

.card-date {
    @apply text-right;
}

#chat-widget {
    @apply fixed bottom-0 right-0 sm:bottom-8 sm:right-8 m-0 sm:m-4 w-full h-full sm:w-[400px] sm:h-[600px] bg-white rounded-none sm:rounded-2xl shadow-2xl flex flex-col transition-transform duration-300 ease-in-out z-50;
    transform: translateY(100%);
}

#chat-widget.active {
    transform: translateY(0);
}

#chat-header {
    @apply flex items-center justify-between p-4 bg-slate-50 border-b border-slate-200 rounded-t-none sm:rounded-t-2xl;
}

#chat-messages {
    @apply flex-1 p-4 overflow-y-auto space-y-4;
}

.chat-message {
    @apply flex items-end max-w-xs;
}

.chat-bubble {
    @apply px-4 py-2 rounded-2xl;
}

.user-message {
    @apply ml-auto;
}

.user-message .chat-bubble {
    @apply bg-blue-600 text-white rounded-br-lg;
}

.bot-message {
    @apply mr-auto;
}

.bot-message .chat-bubble {
    @apply bg-slate-100 text-slate-800 rounded-bl-lg;
}

#chat-form {
    @apply p-4 border-t border-slate-200 bg-white rounded-b-none sm:rounded-b-2xl;
}

#chat-input-wrapper {
    @apply flex items-center space-x-2;
}

#chat-input {
    @apply w-full bg-slate-100 border-transparent rounded-full py-2 px-4 focus:outline-none focus:ring-2 focus:ring-blue-500 transition-shadow;
}
